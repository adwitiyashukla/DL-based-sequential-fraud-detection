# Install PyTorch first, from the CPU-only index. The default PyPI wheel is the
# CUDA build, roughly 2.5 GB, which is dead weight on a machine without an
# NVIDIA GPU. The CPU wheel is about 200 MB.
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# torch is pinned without the "+cpu" local tag because that tag is not
# resolvable from PyPI. The constraint below is satisfied by the CPU build.

torch==2.13.0
pandas==3.0.5
numpy==2.5.1
scikit-learn==1.9.0
lightgbm==4.7.0
matplotlib==3.11.1
